# Frontend GITT Inventario

Aplicacion React + Vite del Sistema Web de Gestion de Inventario Tecnologico para la FISEI.

```bash
npm install
npm run dev
```

La URL de la API se configura en `.env` con `VITE_API_URL`.
